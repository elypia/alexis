# {param} is using parameter expanstion, inserts parameter values.
# ${expression} is using expression expansion, it's evaluated by the Java Expression Language API.

com.elypia.commandler.constraints.Period.message=Must be between {min} and {max} ${unit.toString().toLowerCase()}.
com.elypia.commandler.constraints.Option.message=Must be one of {value}
